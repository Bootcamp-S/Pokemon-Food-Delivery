type: http
name: Defaults to pikachu
request:
  method: GET
  url: {{baseUrl}}/api/index
tests:
  - name: returns pikachu
    script: |
      const b = response.json();
      expect(b.name).to.equal('pikachu');
